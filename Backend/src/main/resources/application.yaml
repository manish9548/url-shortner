spring:
  application:
    name: project
  datasource:
    url: jdbc:mysql://localhost:3306/url_shortner?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true